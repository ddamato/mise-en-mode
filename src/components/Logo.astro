---
import logo from '@components/Logo.svg?raw';
---

<style>
  span {
    aspect-ratio: 1;
		max-width: calc(var(--unit) * 8);
		width: 100%;
		display: flex;
		margin: auto;
		font-size: min(4rem, var(--unit) * 2);
		filter: drop-shadow(.04em .04em #ffb53d);
  }
</style>

<span set:html={logo} />